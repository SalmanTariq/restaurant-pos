export enum OrderType {
  TAKEAWAY = 'takeaway',
  TABLE = 'table',
}

export enum OrderStatus {
  OPEN = 'open',
  PAID = 'paid',
  COMPLETED = 'completed',
  CANCELLED = 'cancelled',
  CLOSED = 'closed',
}

export enum PaymentMethod {
  CASH = 'cash',
  CARD = 'card',
}
